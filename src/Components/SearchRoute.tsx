import React from 'react'
import { Route, Redirect } from "react-router-dom";
/*

interface IRouteProps {
  redirection:boolean
  location:string
}

const searchTerm = useSelector(selectSearch)

const SearchRoute:React.FC<IRouteProps> = (props) => {
  const { redirection, location } = props
  if(redirection) {
    return <Redirect  from="*" 
                      to={{
                      pathname:"/search",
                      state:{location}
                      }}/>
  }
  return (<Route {...props}/>)
}
*/

export default  {}